import React from 'react';
import FilterSortButton from '../components/FilterSortButton';
import {compose} from 'recompose';
// import { wit} from '../utils/enhancers'

const enhance = compose();

export default enhance(FilterSortButton);
